# 📋 Пошаговая инструкция по настройке

## Шаг 1: Создание Telegram бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и создайте бота
4. Сохраните токен бота (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Получение вашего Telegram ID

1. Найдите бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте команду `/start`
3. Бот покажет ваш ID (например, `123456789`)
4. Сохраните этот ID - он нужен для админ-доступа

## Шаг 3: Настройка Backend

1. Откройте папку `backend`
2. Создайте файл `.env` (скопируйте из `.env.example`)
3. Заполните переменные:
   ```env
   BOT_TOKEN=ваш_токен_от_BotFather
   ADMIN_IDS=ваш_telegram_id
   PORT=3000
   ```
4. Установите зависимости:
   ```bash
   npm install
   ```

## Шаг 4: Запуск Backend локально (для тестирования)

```bash
# Запустить только API сервер
npm start

# Или запустить бота и сервер одновременно
npm run both
```

## Шаг 5: Деплой Backend на хостинг

### Вариант A: Railway (рекомендуется)

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Подключите ваш репозиторий
4. Выберите папку `backend`
5. В настройках проекта добавьте переменные окружения:
   - `BOT_TOKEN`
   - `ADMIN_IDS`
   - `PORT` (Railway установит автоматически)
6. Railway автоматически задеплоит ваш проект
7. Скопируйте URL вашего проекта (например, `https://your-project.railway.app`)

### Вариант B: Render

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Нажмите "New" → "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - **Name**: manicure-backend
   - **Root Directory**: backend
   - **Build Command**: `npm install`
   - **Start Command**: `npm run both`
5. Добавьте переменные окружения
6. Нажмите "Create Web Service"
7. Скопируйте URL (например, `https://manicure-backend.onrender.com`)

## Шаг 6: Настройка Mini App

1. Откройте файл `app.js`
2. Найдите строку:
   ```javascript
   const API_URL = 'https://your-backend-url.com/api';
   ```
3. Замените на URL вашего backend + `/api`:
   ```javascript
   const API_URL = 'https://your-project.railway.app/api';
   ```

## Шаг 7: Деплой Mini App на GitHub Pages

1. Создайте новый репозиторий на GitHub
2. Загрузите файлы:
   - `index.html`
   - `styles.css`
   - `app.js`
3. В настройках репозитория:
   - Перейдите в "Settings" → "Pages"
   - В "Source" выберите ветку `main` и папку `/ (root)`
   - Нажмите "Save"
4. Через несколько минут ваш Mini App будет доступен по адресу:
   `https://ваш-username.github.io/название-репозитория/`

## Шаг 8: Настройка Web App в Telegram

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newapp`
3. Выберите вашего бота
4. Введите название: `Маникюрный салон`
5. Введите описание: `Запись на маникюр`
6. Загрузите иконку (опционально, размер 640x360px)
7. Введите URL вашего Mini App: `https://ваш-username.github.io/название-репозитория/`
8. Готово! Теперь в вашем боте появится кнопка "Open App"

## Шаг 9: Тестирование

1. Откройте вашего бота в Telegram
2. Нажмите кнопку "Open App" или отправьте команду `/start`
3. Проверьте работу всех функций:
   - Просмотр услуг
   - Создание записи
   - Просмотр отзывов
4. Если вы админ, проверьте админ-панель:
   - Добавление услуг
   - Настройка расписания
   - Просмотр записей
   - Экспорт в Excel

## 🔧 Решение проблем

### Mini App не открывается
- Проверьте URL в настройках бота
- Убедитесь, что GitHub Pages включен
- Проверьте консоль браузера (F12) на ошибки

### Backend не отвечает
- Проверьте, что сервер запущен
- Проверьте переменные окружения
- Проверьте логи на хостинге

### Бот не получает сообщения
- Проверьте токен бота
- Убедитесь, что бот запущен
- Проверьте логи бота

### CORS ошибки
- Убедитесь, что в `server.js` включен CORS
- Проверьте, что URL в `app.js` правильный

## 📞 Поддержка

Если возникли проблемы, проверьте:
1. Логи backend сервера
2. Логи Telegram бота
3. Консоль браузера (F12)
4. Настройки переменных окружения
