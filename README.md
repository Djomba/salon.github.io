# 💅 Mini App для маникюрного салона

Красивое Mini App для записи на маникюр с интеграцией Telegram бота и админ-панелью.

## ✨ Возможности

- 🎨 Красивый интерфейс в стиле Telegram с бело-розовым фоном и анимированными эмоджи
- 👤 Профиль пользователя
- 💅 Каталог услуг
- 📅 Система записи с выбором даты и времени
- ⭐ Отзывы клиентов
- 🔐 Админ-панель для управления услугами и расписанием
- 📊 Экспорт записей в Excel
- 🤖 Интеграция с Telegram ботом
- 💾 Сохранение данных в базу данных

## 🏗️ Архитектура

```
Mini App (GitHub Pages)
    ↓
Backend API (Node.js)
    ↓
Telegram Bot
    ↓
База данных (JSON файлы)
```

## 📋 Требования

- Node.js 16+ 
- Telegram Bot Token (получить у @BotFather)
- GitHub аккаунт (для хостинга Mini App)

## 🚀 Установка и настройка

### 1. Настройка Backend

```bash
cd backend
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в папке `backend`:

```env
BOT_TOKEN=ваш_токен_бота_от_BotFather
ADMIN_IDS=123456789,987654321
PORT=3000
```

Где:
- `BOT_TOKEN` - токен вашего Telegram бота
- `ADMIN_IDS` - ID администраторов через запятую (можно узнать у @userinfobot)
- `PORT` - порт для backend сервера (по умолчанию 3000)

### 3. Запуск Backend

```bash
# Запустить только API сервер
npm start

# Или запустить бота и сервер одновременно
npm run both
```

### 4. Настройка Mini App

1. Откройте файл `app.js`
2. Найдите строку:
```javascript
const API_URL = 'https://your-backend-url.com/api';
```
3. Замените на URL вашего backend сервера

### 5. Деплой на GitHub Pages

1. Создайте новый репозиторий на GitHub
2. Загрузите файлы `index.html`, `styles.css`, `app.js`
3. В настройках репозитория включите GitHub Pages
4. Выберите ветку `main` и папку `/root`

### 6. Настройка Telegram бота

1. Создайте бота через @BotFather
2. Получите токен бота
3. Установите Web App:
   ```
   /newapp
   Выберите вашего бота
   Введите название: Маникюрный салон
   Введите описание: Запись на маникюр
   Загрузите иконку (опционально)
   Введите URL: https://ваш-username.github.io/название-репозитория/
   ```

## 📁 Структура проекта

```
.
├── index.html          # Главная страница Mini App
├── styles.css          # Стили
├── app.js              # JavaScript логика
├── backend/
│   ├── server.js       # Express API сервер
│   ├── bot.js          # Telegram бот
│   ├── package.json    # Зависимости
│   └── data/           # База данных (JSON файлы)
│       ├── services.json
│       ├── bookings.json
│       ├── schedule.json
│       └── reviews.json
└── README.md           # Документация
```

## 🔐 Админ-панель

Админ-панель доступна только пользователям, чьи Telegram ID указаны в `ADMIN_IDS`.

### Возможности админа:

1. **Управление услугами**
   - Добавление новых услуг
   - Редактирование существующих
   - Удаление услуг

2. **Управление расписанием**
   - Установка доступного времени для каждой даты
   - Формат: `10:00, 12:00, 14:00, 16:00, 18:00, 20:00`

3. **Просмотр записей**
   - Список всех записей
   - Экспорт в Excel

## 🤖 Команды бота

### Для всех пользователей:
- `/start` - Начать работу с ботом

### Для админов:
- `/bookings` - Просмотр всех записей
- `/excel` - Скачать Excel файл с записями
- `/edit <ID>` - Редактировать запись
- `/editdata <ID>|<Имя>|<Телефон>|<Дата>|<Время>|<Комментарий>` - Обновить данные записи

## 📊 API Endpoints

### Публичные:
- `GET /api/services` - Получить все услуги
- `GET /api/schedule/:date` - Получить расписание на дату
- `POST /api/bookings` - Создать запись
- `GET /api/reviews` - Получить отзывы
- `POST /api/reviews` - Создать отзыв

### Админские:
- `GET /api/admin/check/:userId` - Проверить статус админа
- `GET /api/admin/services` - Получить все услуги
- `POST /api/admin/services` - Создать услугу
- `PUT /api/admin/services/:id` - Обновить услугу
- `DELETE /api/admin/services/:id` - Удалить услугу
- `POST /api/admin/schedule` - Сохранить расписание
- `GET /api/admin/schedule` - Получить расписание
- `DELETE /api/admin/schedule/:date` - Удалить расписание
- `GET /api/admin/bookings` - Получить все записи
- `GET /api/admin/export/excel` - Экспорт в Excel

## 🔄 Как это работает

1. Пользователь открывает Mini App в Telegram
2. Заполняет форму записи
3. Данные отправляются на backend API
4. Backend сохраняет данные в базу
5. Backend отправляет уведомление в Telegram бота
6. Админ получает уведомление в боте и может редактировать запись

## 🌐 Деплой Backend

### Вариант 1: Railway
1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Добавьте переменные окружения
5. Деплой произойдет автоматически

### Вариант 2: Render
1. Зарегистрируйтесь на [Render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Укажите команду запуска: `npm start`
5. Добавьте переменные окружения

### Вариант 3: VPS
1. Установите Node.js на сервер
2. Склонируйте репозиторий
3. Установите зависимости: `npm install`
4. Настройте переменные окружения
5. Запустите через PM2: `pm2 start server.js`

## 📝 Примечания

- База данных использует JSON файлы для простоты
- Для продакшена рекомендуется использовать PostgreSQL или MongoDB
- Все данные хранятся в папке `backend/data/`
- Не забудьте настроить CORS для вашего домена

## 🐛 Решение проблем

### Mini App не загружается
- Проверьте URL в настройках бота
- Убедитесь, что GitHub Pages включен
- Проверьте консоль браузера на ошибки

### Backend не отвечает
- Проверьте, запущен ли сервер
- Проверьте переменные окружения
- Проверьте логи сервера

### Бот не получает сообщения
- Проверьте токен бота
- Убедитесь, что бот запущен
- Проверьте логи бота

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Создано с ❤️ для маникюрного салона
